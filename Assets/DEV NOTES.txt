* upgrade the old ui_linked icon system cuz its confusing (ui_worldspaceelement is better)
* split the entity packaging into multiple packets
* make the entity position updates a batch-based system

* make the command request perms check server-side, not client-side (security risk lol)